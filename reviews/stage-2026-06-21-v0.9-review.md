# 阶段 Review：2026-06-21 v0.9

## 本轮新增能力

- 新增 `reproduction-data-analyst` skill，覆盖论文代码复现、数据分析核验、指标差异排查和可复现记录。
- 新增 `reproduction-mismatch-minimal` adversarial fixture，模拟本地结果与论文 Table 3 不一致。
- 新增对应 eval output，要求拒绝伪复现，并优先排查 dataset version、split、seed、environment 和 metric implementation。

## 真正提升 Codex 科研能力的部分

- 复现结果必须绑定 paper target、数据版本、命令、config、seed、环境和输出路径。
- mismatch 不会被写成成功；输出明确 target/local/delta/tolerance。
- 下一步是最小诊断 run，而不是随机改代码或笼统重跑。

## 仍需警惕的部分

- Fixture 没有真实 repo、依赖安装失败、GPU nondeterminism 或长时训练成本。
- 数据 manifest、checksum 和环境 lockfile 仍需真实项目提供。
- 复现 skill 能定位可疑原因，但不能替代领域判断或统计显著性检验。

## 删除或重写标准

- 如果本地结果超出容差仍声明 reproduced，应判失败。
- 如果没有检查数据版本/split/seed 就开始改模型代码，应重写。
- 如果 run log 缺命令、CWD、config、输出路径，应先补记录再继续。

## 下一步

1. 用真实 repo 或 notebook 验证 `reproduction-data-analyst`。
2. 新增一个数据分析 fixture：统计结论受缺失值处理或分组泄漏影响。
3. 独立会话重跑 8 个 fixture，把差异写入下一份 validation report。
